<?xml version="1.0" encoding="UTF-8"?>
<workspaceFilter version="1.0">
    <!-- Scoped to the page only, mode=merge: never remove sibling nodes
         (e.g. /content/iggroup/configuration). The iggroup and
         language-masters ancestor pages already exist in AEM. -->
    <filter root="/content/iggroup/language-masters/en" mode="merge"/>
</workspaceFilter>
